import 'package:flutter/material.dart';

class TopTeachersScreen extends StatelessWidget {
  static const String path = "/topTeachers";
  const TopTeachersScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}